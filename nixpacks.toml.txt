# ================= SETUP =================
[phases.setup]
nixPkgs = [
  "python311",
  "ffmpeg-full",   # ✅ FIXED (IMPORTANT)
  "gcc",
  "libsodium",
  "opus",
  "nodejs",
  "git"
]

# ================= INSTALL =================
[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements.txt"
]

# ================= START =================
[start]
cmd = "python bot.py"
